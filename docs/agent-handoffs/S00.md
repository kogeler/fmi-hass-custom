# Session S00 Handoff

## Metadata

- Session: S00 - Bootstrap, refresh baseline, and create initial `AGENTS.md`
- Status: DONE
- Started UTC: 2026-07-31T06:44:16Z
- Completed UTC: 2026-07-31T06:55:14Z
- Base commit: `77a265cf910050aa6e2e37a4eb57d951bddc2910`
- Final commit(s): `389c67901085efe6b53bee1474a2f89bfab20867`
- Executor: OpenAI Codex

## Objective

Create durable project memory and replace the plan's assumptions with a verified repository and ecosystem baseline, without changing production behavior.

## Changes made

- Added a concise root `AGENTS.md` with session, testing, Git safety, generated-freeze, and handoff rules.
- Added `docs/maintenance/BASELINE.md` with the verified fork/upstream relationship, repository responsibilities, dependency/CI state, issue/PR inventory, ecosystem versions, command results, risks, and authoritative links.
- Updated the plan with the verified S00 refresh, tracker state, immediate plan-correction rule, hardware-token avoidance, owner-controlled commits, and full `pip freeze` policy for root `requirements.txt`.
- Made no production-code, dependency, workflow, or runtime-behavior change.

## Root causes and decisions

- Local `master`, `origin/master`, and upstream `master` are identical at `77a265cf910050aa6e2e37a4eb57d951bddc2910`; the fork has no fork-only commit.
- The open inventory remains six issues (`#110`, `#111`, `#112`, `#114`, `#115`, `#117`) and PR `#116`; no new closure-matrix row was needed.
- Home Assistant `2026.7.4` is stable, `2026.8.0b2` is beta, and current source requires Python `>=3.14.2`; existing CI on 3.12/3.13 is not a current compatibility matrix.
- Core has no `homeassistant/components/fmi` path, so the plan is not blocked by a domain conflict.
- HACS still documents `content_in_root: true` as valid. Actual HACS/hassfest execution and the final layout decision remain correctly assigned to S01.
- Latest `fmi-weather-client` is still 1.0.0. Its wheel/API was inspected without installing or upgrading it; contract tests remain prerequisites for S03.
- Per owner instruction, Codex did not create a commit and recorded `OWNER_TO_COMMIT`.

## Tests and checks run

| Command | Result | Notes |
|---|---|---|
| `python --version` | FAIL | The `python` command is not installed in the starting environment. |
| `flake8 --version` | FAIL | `flake8` is not installed in the starting environment. |
| `pylint --version` | FAIL | `pylint` is not installed in the starting environment. |
| `flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics` | FAIL | Existing workflow command could not start because `flake8` is absent. |
| `flake8 . --count --exit-zero --statistics` | FAIL | Existing workflow command could not start because `flake8` is absent. |
| `pylint *.py --init-hook='import sys; sys.path.append(".")'` | FAIL | Existing workflow command could not start because `pylint` is absent. |
| `python3 --version` | PASS | Python 3.13.5 is available, but is below current Home Assistant's Python requirement. |
| `test -s AGENTS.md` | PASS | Root `AGENTS.md` is nonempty. |
| `wc -c -l AGENTS.md` | PASS | 38 lines and 2,633 bytes, below the project instruction size limit. |
| Baseline URL status loop using `curl -L` | PASS | Every authoritative and issue/PR URL recorded in `BASELINE.md` returned HTTP 200. |
| `git diff --check` | PASS | No whitespace errors reported in tracked diffs. |
| `git status --short` | PASS | Only S00 documentation/plan files are untracked; no production file changed. |

Never record PASS for a command that was not run.

## Files materially changed

- `AGENTS.md` - initial durable agent instructions.
- `plans/CODEX_FMI_HASS_MAINTENANCE_PLAN.md` - verified baseline/tracker and owner-requested operating rules; moved to `plans/` during S01.
- `docs/maintenance/BASELINE.md` - authoritative S00 repository and ecosystem baseline.
- `docs/agent-handoffs/S00.md` - session evidence and S01 handoff.

## Issue/requirement mapping

- S00 requirements 1-5 - fork/upstream, issues/PRs, current dependencies/ecosystem, FMI terms, and Core domain were verified.
- S00 requirements 6-9 - baseline, handoff directory, and root `AGENTS.md` were created.
- S00 requirement 10 - the existing issue matrix remains current; verified values were added to Section 5.
- Owner additions - plan now requires immediate corrections, no hardware-token Git operations, owner-created commits, and complete generated `requirements.txt` freeze after upgrades.

## Remaining risks or blockers

- The existing lint commands cannot run until S01 provisions a reproducible environment; this is a documented baseline failure, not an S00 blocker.
- Python 3.13.5 on the host cannot run current Home Assistant, which requires Python 3.14.2 or newer.
- HACS/hassfest acceptance of the current checkout has not been executed; S01 owns that validation and the layout decision.
- Root `requirements.txt` is intentionally unchanged in S00 and remains unfrozen until the S01/S03 dependency workflow is established.

## Instructions for the next session

- Read the plan, `AGENTS.md`, this handoff, and `docs/maintenance/BASELINE.md` before S01.
- Provision the current Home Assistant Python requirement without relying on the host's Python 3.13.5.
- Preserve the separation between `manifest.json` runtime requirements, the direct development/test dependency source, and the complete generated root `requirements.txt` freeze.
- Test the root `content_in_root` layout with current HACS/hassfest before deciding whether any layout migration is necessary.
- Do not use authenticated/SSH Git operations and do not create a commit.

## Suggested commit message

`S00: document verified maintenance baseline`
