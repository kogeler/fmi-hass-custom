<!-- Copyright (c) 2026 kogeler. SPDX-License-Identifier: MIT. -->

# P02-S04 — Documentation And Final Verification

- Date: 2026-08-17
- Baseline commit: `2c884b0402a00c59e9276ac25610b242b403a849`
- Branch: `feat/map-location`
- Working tree at start: green uncommitted S00-S03 changes per the continuous-run instruction
- Suggested owner commit: `P02-S04: deliver map and FMI place location flows for 1.1.0`
- Status: DONE

## Objective

Synchronize every user and maintainer contract with the implemented location experience, retain
the existing `1.1.0` release train, and prove release readiness across the complete local, audit,
live, and moving-compatibility matrix.

## Baseline and evidence

The current user guide still instructed users to enter visible latitude/longitude values, and the
README described reconfiguration only as changing coordinates. Current maintenance contracts for
runtime, reconfiguration, security/privacy, development, and live FMI were otherwise already
updated by S01-S03.

S04 also inventoried every translation and found that `translations/fi.json` retained the removed
raw-coordinate steps. Hassfest permits stale keys in an optional locale, so this could not be left
to automated validation alone. D015 and the S04 work list were added immediately, and Finnish was
synchronized with the source and English key/step structure before final validation.

No P02 change touched `.github/workflows/`, direct dependency manifests, any of the three locks,
`.version`, the manifest version, or `hacs.json`. P02 therefore required neither a workflow change,
dependency refresh, HACS-floor change, nor another release increment.

## Changes made

- Rewrote installation/configuration/reconfigure/troubleshooting/privacy guidance around the
  Home-defaulted map and FMI place search with explicit map confirmation.
- Updated the README limitation to point to both supported reconfigure paths.
- Updated the migration contract to cover post-migration place search, map confirmation, options,
  registry, and identity preservation.
- Synchronized Finnish menu, map, place, confirmation, and error translations and verified exact
  config-key parity with English.
- Kept the existing concise `1.1.0` changelog entry; no numeric version was copied into current user
  documentation.
- Removed only the now-completed location-selection TODO after S00-S04, R01-R10, documentation,
  and the full matrix were complete. The unrelated cryptography and Nominatim items remain.

## Plan corrections and deviations

- D015 corrects the original English-only translation scope. The repository already shipped a
  Finnish locale, so leaving its raw coordinate forms would violate R09 despite hassfest passing.
- The stable compatibility resolver advanced from the reference HA 2026.8.1/helper 0.13.355 to
  published HA 2026.8.2/helper 0.13.356. It passed without changing the reproducible reference lock;
  this is exactly the moving-signal contract, not evidence to raise the HACS floor.
- No newer installable Home Assistant prerelease existed. The prerelease target returned its
  designed successful informational skip instead of inventing a beta support result.
- HACS local validation remained an explicit skip because no token/repository/ref inputs were
  provided. Hassfest, actionlint, layout/distribution, and all local integration validation passed;
  the remote HACS PR job remains the repository-owned external check.

## Decisions

- D015: all existing locales must match an exposed flow, even when hassfest does not reject stale
  optional-locale keys.
- D007 remains satisfied: `.version` and manifest are `1.1.0`, with one dated changelog section.
- No dependency or CI decision changed because those files have no P02 diff.

## Tests and checks run

| Command/check | Result | Evidence/notes |
|---|---|---|
| `make check` | PASS | Ruff/Pylint, mypy, Bandit, syntax, ShellCheck, 327-test coverage, network block, version, confinement, freeze reproduction, validators, and dependency snapshot all passed |
| `make ci` | PASS | complete `make check` repeated; dependency audit accepted only the 3 exact reviewed upstream findings |
| `make test-full` (through both aggregate gates) | PASS | 327 passed; 99.41% aggregate coverage versus 95% gate |
| `make validate` (through both aggregate gates) | PASS | 5 local validation tests, actionlint, hassfest including Finnish; HACS explicitly skipped without remote inputs |
| `make live` | PASS | 4 passed; 7 actual FMI attempts of the 12-attempt ceiling |
| `make compatibility-stable` | PASS | HA 2026.8.2/helper 0.13.356; `pip check` clean; 327 passed with auto xdist |
| `make compatibility-prerelease` | PASS/SKIP | no prerelease newer than stable 2026.8.2; designed informational skip |
| `make licenses` | PASS | complete installed inventory produced; dependency graph itself was unchanged by P02 |
| Translation leaf-key comparison | PASS | English 29, Finnish 29; no missing or extra config leaves |
| Version mirror check | PASS | `.version` and manifest both `1.1.0`; `make version-check` passed in each aggregate gate |
| `git diff --check` | PASS | no whitespace errors before final report/tracker update |

`make check` executed its explicit `format-check`, `lint`, `type-check`, `bandit`, `syntax`,
`shellcheck`, `test-full`, `test-network-block`, `version-check`, `confinement-test`, `freeze-check`,
`validate`, and `dependency-snapshot` prerequisites on the current tree. `make ci` repeated that
contract after validator image verification and then ran the reviewed online audit.

## Files materially changed

- `docs/USER_GUIDE.md`, `README.md`: current user-facing setup, move, errors, and privacy behavior.
- `custom_components/fmi/translations/fi.json`: previously omitted existing locale synchronized.
- `docs/maintenance/MIGRATIONS.md`, `RECONFIGURATION.md`: current post-migration and authoritative
  reconfigure contract.
- `TODO.md`: only the completed location-selection item removed.
- `plans/P02/PLAN.md`, `S04.md`: D015, final evidence, and complete tracker state.

## Requirement mapping

- R09: DONE — source strings, English/Finnish translations, user guide, README, maintenance
  contracts, agent rules, and existing changelog all match the implemented behavior.
- R10: DONE — local/CI/audit/live/stable gates pass; prerelease accurately skips; no catalog,
  station selector, manual coordinate form, third-party forward geocoder, dependency, or workflow
  scope entered P02.

## Remaining risks or blockers

None owned by P02. The repository still intentionally tracks the exact Home Assistant-pinned
cryptography advisories and public Nominatim scale limitation in `TODO.md`. Remote PR checks,
including authenticated HACS validation, remain owner/GitHub state rather than a local result.

## Final handoff

P02 is complete. The owner should review the complete working-tree diff and commit it without
splitting identity-sensitive code from its tests/contracts. Suggested commit message:

```text
P02-S04: deliver map and FMI place location flows for 1.1.0
```
